// Week 2 Thursday
// HTTP verbs and accepting user data

// taking user data in a form and saving it to a file

// GET: asks for an existing resource
// POST: sends data to create a new resource
// PUT: sends data to update existing resource
// DELETE: asks for an exisitng resource to be deleted

// link at the bottom of the gitbook page for these HTTP definitions

// to accept user data we use a form
// < form action = "http://foo.com/greetings" method = "get" >
// for must contain your values
// such as
// < input name = "say" value = "Hi" >
// < /form>

// npm i
// npm i body-parser --save
// without this we haven't installed body parser so it will not work
// when we try to use it in out document

//npm i node-inspector --save-dev
//

//npm i nodemon --save-dev

// added to scripts
"debug" = ""
